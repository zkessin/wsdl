{erl_opts, [debug_info]}.
{deps, [{soap, "1.1.0"}]}.

{plugins, [rebar_mix]}.
{provider_hooks, [
  {post, [{compile, {mix, consolidate_protocols}}]}
]}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [wsdl]}
]}.
